name: 'Deploy with Tailscale Access'

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      audience:
        description: 'Tailscale OIDC audience (api.tailscale.com/YOUR_TAILNET_ID)'
        required: true
        default: api.tailscale.com/kHWmv3fFFX11CNTRL
      client_id:
        description: 'Tailscale client ID (tailnet-id/client-id)'
        required: true
        default: TdWjeTt8mN11CNTRL/kHWmv3fFFX11CNTRL
      tailnet:
        description: 'Tailnet name'
        required: false
        default: '_lbr_sandbox'
      tags:
        description: 'Comma-separated tags for devices'
        required: false
        default: 'ci'

jobs:
  # Test the raw OIDC flow first (like the example you provided)
  test-oidc-flow:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Get OIDC token from GitHub Actions
        run: |
          echo "🔑 Getting JWT token..."
          echo "Audience: ${{ inputs.audience }}"
          echo "Client ID: ${{ inputs.client_id }}"

          JWT=$(curl -H "Authorization: Bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=${{ inputs.audience }}" | jq -r '.value')

          echo "JWT length: ${#JWT}"
          echo "JWT first 50 chars: ${JWT:0:50}..."
          echo "JWT=$JWT" >> $GITHUB_ENV
          
      - name: Debug JWT Claims
        run: |
          echo "Decoding JWT to check claims..."
          JWT_PAYLOAD=$(echo "$JWT" | cut -d. -f2)
          JWT_PAYLOAD_PADDED=$(printf "$JWT_PAYLOAD"; printf "%*s" $((4 - ${#JWT_PAYLOAD} % 4)) | tr ' ' '=')
          echo "$JWT_PAYLOAD_PADDED" | base64 -d | jq .

      - name: Perform OIDC token exchange
        run: |
          echo "🔄 Performing token exchange..."
          
          # Show what we're sending
          echo "Client ID: ${{ inputs.client_id }}"
          echo "JWT length: ${#JWT}"
          
          # Perform the OIDC token exchange with Tailscale
          RESPONSE=$(curl -X POST https://api.tailscale.com/api/v2/oauth/token-exchange \
            -H "Content-Type: application/x-www-form-urlencoded" \
            -d "client_id=${{ inputs.client_id }}" \
            -d "jwt=$JWT" \
            -w "HTTP_CODE:%{http_code}")
          
          # Extract HTTP code and response body
          HTTP_CODE=$(echo "$RESPONSE" | grep -o "HTTP_CODE:[0-9]*" | cut -d: -f2)
          RESPONSE_BODY=$(echo "$RESPONSE" | sed 's/HTTP_CODE:[0-9]*$//')
          
          echo "HTTP Status: $HTTP_CODE"
          echo "Response: $RESPONSE_BODY"
          
          if [ "$HTTP_CODE" != "200" ]; then
            echo "❌ Token exchange failed!"
            exit 1
          fi
          
          # Extract access token
          ACCESS_TOKEN=$(echo $RESPONSE_BODY | jq -r '.access_token')
          echo "ACCESS_TOKEN=$ACCESS_TOKEN" >> $GITHUB_ENV
          echo "✅ Access token obtained: ${ACCESS_TOKEN:0:10}..."

      - name: Test API request
        run: |
          echo "🌐 Testing API request..."
          curl -v https://api.tailscale.com/api/v2/tailnet/${{ inputs.tailnet }}/devices \
            --header "Authorization: Bearer ${ACCESS_TOKEN}"

  # Test using your custom action
  test-custom-action:
    runs-on: ubuntu-latest
    needs: test-oidc-flow  # Only run if the raw OIDC test passes
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure Tailscale Credentials (Custom Action)
        id: tailscale-auth
        uses: ./configure-tailscale-credentials
        with:
          audience: ${{ inputs.audience }}
          client-id: ${{ inputs.client_id }}
          tailnet: ${{ inputs.tailnet }}
          tags: ${{ inputs.tags }}

      - name: Connect to Tailscale
        uses: tailscale/github-action@v2
        with:
          authkey: ${{ steps.tailscale-auth.outputs.ts-auth-key }}
          tags: ${{ inputs.tags }}

      - name: Check Tailscale Connection
        run: |
          echo "Tailscale status:"
          tailscale status
          
          echo "Tailscale IP:"
          tailscale ip -4

      - name: Use Tailscale API
        run: |
          echo "Listing devices in tailnet..."
          curl -H "Authorization: Bearer ${{ steps.tailscale-auth.outputs.ts-access-token }}" \
               https://api.tailscale.com/api/v2/tailnet/-/devices